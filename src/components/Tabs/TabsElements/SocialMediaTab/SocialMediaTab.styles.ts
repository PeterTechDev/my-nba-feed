import styled from "styled-components";

export const StyledSocialMediaTab = styled.div`
  padding: 1rem;
  color: var(--gray-100);

  padding: 0;
  margin: 0 auto;

  h3 {
    margin-bottom: 1rem;
    font-size: 1.5rem;
  }
`;

export const LoadingMessage = styled.div`
  padding: 1rem;
  font-size: 1.2rem;
  color: var(--gray-400);
`;

export const ErrorMessage = styled.div`
  padding: 1rem;
  font-size: 1.2rem;
  color: var(--danger);
`;
